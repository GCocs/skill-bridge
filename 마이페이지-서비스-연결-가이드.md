# 마이페이지 서비스 연결 가이드

## 1. 목적

이 문서는 현재 구현된 Skill Bridge 마이페이지를 다른 프론트엔드 프로젝트로 이식하고, 기존 MSA 엔드포인트에 연결하는 방법을 설명한다.

현재 마이페이지는 다음 두 영역으로 구성된다.

- `내 정보`: 모든 사용자에게 노출
- `추천 강의`: `STUDENT` 사용자에게만 노출

새로운 백엔드 API나 기능을 추가하지 않으며, 아래의 기존 API만 사용한다.

| 용도 | API | 호출 대상 |
| --- | --- | --- |
| 로그인 사용자 정보 | `GET /api/users/me` | user-service |
| 사용자 맞춤 강의 추천 | `GET /api/recommend/{userId}` | recommend-service |

운영 환경에서는 브라우저가 개별 MSA 포트를 직접 호출하기보다, 기존 API Gateway 또는 서버 프록시의 동일 출처 `/api` 경로를 통해 호출하는 구성을 권장한다.

---

## 2. 이식할 프론트엔드 파일

### 필수 파일

```text
src/
├─ views/
│  └─ MyPageView.vue
├─ components/
│  ├─ UserInfoCard.vue
│  ├─ CourseRecommendations.vue
│  └─ CourseCard.vue
├─ composables/
│  └─ useMyPage.js
├─ services/
│  └─ myPageApi.js
└─ data/
   └─ mockApi.js
```

- `MyPageView.vue`: 마이페이지의 데이터와 두 영역을 조립하는 라우트 화면이다.
- `UserInfoCard.vue`: 사용자 정보를 표시한다.
- `CourseRecommendations.vue`: 추천 응답의 메시지와 강의 목록을 표시한다.
- `CourseCard.vue`: 추천 강의 한 건을 표시한다.
- `useMyPage.js`: 사용자 조회, 역할 판별, 추천 조회 순서를 관리한다.
- `myPageApi.js`: mock/실제 API 호출을 전환하는 데이터 계층이다.
- `mockApi.js`: 백엔드와 같은 형식의 개발용 더미 응답을 제공한다.

### 프로젝트 상황에 따라 선택할 파일

```text
src/components/AppHeader.vue
src/assets/base.css
public/skill-bridge-logo.png
```

- 대상 프로젝트에 공통 헤더가 있으면 `AppHeader.vue`와 로고는 이식하지 않는다.
- `base.css`는 브랜드 색상 및 폰트 토큰이 필요할 때 이식한다. 기존 전역 CSS가 있다면 필요한 CSS 변수만 병합한다.
- 대상 프로젝트의 `App.vue`는 덮어쓰지 않는다. 현재 구현의 `MyPageView.vue`를 기존 라우터에 등록한다.

다음 디렉터리와 파일은 프론트 이식 대상이 아니다.

```text
user-service/
recommend-service/
브랜드 가이드.html
package-lock.json
src/stores/
```

`src/router/` 전체를 복사할 필요는 없지만, 아래 설명처럼 대상 프로젝트의 기존 라우터에 `/mypage` 경로를 추가해야 한다.

---

## 3. 데이터 호출 흐름

마이페이지 진입 시 호출 순서는 다음과 같다.

```text
마이페이지 진입
    │
    └─ GET /api/users/me
            │
            ├─ role === STUDENT
            │      └─ GET /api/recommend/{data.id}
            │             └─ 내 정보 + 추천 강의 렌더링
            │
            └─ role === INSTRUCTOR
                   └─ 내 정보만 렌더링
```

`INSTRUCTOR`인 경우 추천 컴포넌트를 렌더링하지 않으며, 추천 API도 호출하지 않는다. 이 분기는 `useMyPage.js`에 구현되어 있다.

---

## 4. API 응답 계약

### 4.1 사용자 정보

요청:

```http
GET /api/users/me
```

응답은 `ApiResponse<UserResponse>` 래퍼 구조다.

```json
{
  "success": true,
  "message": "성공",
  "data": {
    "id": 24,
    "email": "minseo.kim@skillbridge.kr",
    "name": "김민서",
    "role": "STUDENT",
    "createdAt": "2025-03-18T09:24:00"
  }
}
```

필드:

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `success` | `boolean` | 요청 성공 여부 |
| `message` | `string` | 처리 결과 메시지 |
| `data.id` | `number` | 사용자 ID. 추천 API 호출에 사용 |
| `data.email` | `string` | 사용자 이메일 |
| `data.name` | `string` | 사용자 이름 |
| `data.role` | `STUDENT \| INSTRUCTOR` | 화면 및 API 호출 분기 기준 |
| `data.createdAt` | ISO datetime 문자열 | 가입일 |

프론트에서는 반드시 `response.data`를 사용자 객체로 사용해야 한다.

### 4.2 추천 강의

요청:

```http
GET /api/recommend/24
```

추천 응답은 사용자 API와 달리 공통 `ApiResponse` 래퍼가 없는 `RecommendResponse` 구조다.

```json
{
  "userId": 24,
  "recommendedCourses": [
    {
      "id": 101,
      "title": "Vue 3로 완성하는 실전 프론트엔드",
      "description": "Composition API부터 상태 관리까지 익혀요.",
      "category": "FRONTEND",
      "price": 89000,
      "instructorId": 8,
      "enrollmentCount": 1284,
      "status": "PUBLISHED",
      "createdAt": "2026-07-12T14:00:00"
    }
  ],
  "basedOnCategory": "FRONTEND",
  "message": "FRONTEND 카테고리 기반 추천 강의입니다"
}
```

필드:

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| `userId` | `number` | 추천 대상 사용자 ID |
| `recommendedCourses` | `Course[]` | 최대 5개의 추천 강의 |
| `basedOnCategory` | `CourseCategory \| null` | 추천 기준 카테고리. 신규 사용자는 `null` 가능 |
| `message` | `string` | 추천 기준 설명 |

`Course`의 `description`과 `createdAt`은 `null`일 수 있다. `price`는 JSON 직렬화 방식에 따라 숫자 또는 숫자 문자열로 들어와도 표시되도록 구현되어 있다.

지원 카테고리:

```text
BACKEND
FRONTEND
DEVOPS
DATA_SCIENCE
MOBILE
SECURITY
DATABASE
OTHER
```

---

## 5. Mock 데이터로 실행하기

별도 설정이 없으면 mock API를 사용한다.

```env
VITE_USE_MOCK_API=true
VITE_MOCK_USER_ROLE=STUDENT
```

역할별 화면은 다음과 같이 확인할 수 있다.

```text
학생:  http://localhost:5173/
강사:  http://localhost:5173/?role=INSTRUCTOR
```

URL의 `role` 쿼리 값이 `VITE_MOCK_USER_ROLE`보다 우선한다. 이 역할 변경 기능은 mock 모드의 화면 확인 용도이며 실제 사용자 권한을 변경하지 않는다.

더미 데이터가 필요 없는 시점에는 `mockApi.js`와 관련 import를 제거할 수 있다.

---

## 6. 실제 서비스로 전환하기

### 6.1 동일 출처 Gateway를 사용하는 경우

권장 설정:

```env
VITE_USE_MOCK_API=false
VITE_USER_SERVICE_URL=/api
VITE_RECOMMEND_SERVICE_URL=/api
```

실제 호출 URL:

```text
/api/users/me
/api/recommend/{userId}
```

Vite 환경 변수는 애플리케이션 빌드 시 주입된다. 운영 값을 변경했다면 프론트엔드를 다시 빌드해야 한다.

### 6.2 개발 환경에서 서비스 포트를 직접 사용하는 경우

```env
VITE_USE_MOCK_API=false
VITE_USER_SERVICE_URL=http://localhost:8081/api
VITE_RECOMMEND_SERVICE_URL=http://localhost:8085/api
```

직접 호출 방식은 CORS와 인증 헤더 전달 설정이 별도로 필요할 수 있으므로, 실제 배포에서는 Gateway 경유 방식을 우선한다.

---

## 7. 인증 연결 시 주의사항

### user-service

`GET /users/me`는 API Gateway가 전달하는 `X-User-Id` 헤더를 사용한다. 브라우저에서 사용자 ID를 임의로 만들어 보내지 않는다.

```text
브라우저 → API Gateway → X-User-Id 주입 → user-service
```

### recommend-service

recommend-service는 Bearer 토큰을 검증한다. 대상 프론트에 공통 Axios/fetch 클라이언트와 인증 인터셉터가 있다면, `myPageApi.js`의 `getJson`을 해당 클라이언트로 교체한다.

예시:

```js
import apiClient from '@/services/apiClient'

export async function getCurrentUser() {
  const response = await apiClient.get('/api/users/me')
  return response.data
}

export async function getRecommendations(userId) {
  const response = await apiClient.get(`/api/recommend/${userId}`)
  return response.data
}
```

공통 클라이언트는 기존 인증 구현에 따라 다음 헤더를 처리해야 한다.

```http
Authorization: Bearer {accessToken}
```

토큰을 소스 코드나 `VITE_` 환경 변수에 직접 저장하지 않는다. 현재 시스템의 로그인/토큰 저장 정책과 API Gateway 인증 흐름을 그대로 사용한다.

---

## 8. 기존 Vue 프로젝트에 페이지 등록하기

현재 마이페이지 화면은 `src/views/MyPageView.vue`에 분리되어 있다. 대상 프로젝트의 기존 라우터에 다음 경로를 추가한다.

```js
import MyPageView from '@/views/MyPageView.vue'

const routes = [
  {
    path: '/mypage',
    name: 'mypage',
    component: MyPageView,
  },
]
```

대상 프로젝트의 루트 `App.vue`에는 기존과 같이 `RouterView`를 유지한다.

```vue
<template>
  <RouterView />
</template>
```

기존 프로젝트에 공통 레이아웃, 헤더 또는 푸터가 있다면 마이페이지 내부의 `AppHeader`와 `footer`는 제거하고 공통 레이아웃을 사용한다.

---

## 9. 연결 확인 체크리스트

### 공통

- [ ] `/api/users/me`가 `success`, `message`, `data` 래퍼를 반환한다.
- [ ] `data.id`, `data.role`, `data.createdAt` 필드가 존재한다.
- [ ] 새로고침 시에도 인증 정보가 Gateway로 전달된다.
- [ ] 로딩 중 스켈레톤 또는 로딩 화면이 표시된다.
- [ ] API 오류 시 재시도 UI가 표시된다.

### STUDENT

- [ ] 내 정보와 추천 강의가 모두 표시된다.
- [ ] 사용자 응답의 `data.id`로 추천 API를 호출한다.
- [ ] 추천 응답을 공통 래퍼 없이 바로 사용한다.
- [ ] 추천 결과가 0개여도 빈 상태 화면이 표시된다.
- [ ] `basedOnCategory: null`인 응답이 정상 표시된다.

### INSTRUCTOR

- [ ] 내 정보만 표시된다.
- [ ] 추천 강의 컴포넌트가 표시되지 않는다.
- [ ] `/api/recommend/{userId}` 요청이 발생하지 않는다.

---

## 10. 주요 수정 지점 요약

| 변경 상황 | 수정할 파일 |
| --- | --- |
| API 주소 변경 | `src/services/myPageApi.js` 또는 `.env` |
| Axios/인증 인터셉터 연결 | `src/services/myPageApi.js` |
| 더미 사용자·강의 변경 | `src/data/mockApi.js` |
| 역할별 호출 로직 변경 | `src/composables/useMyPage.js` |
| 사용자 정보 UI 변경 | `src/components/UserInfoCard.vue` |
| 추천 목록 UI 변경 | `src/components/CourseRecommendations.vue`, `CourseCard.vue` |
| 브랜드 색상·폰트 변경 | `src/assets/base.css` |

실제 연동에서 가장 먼저 확인할 파일은 `src/services/myPageApi.js`다. 서버 주소와 인증 클라이언트만 대상 프로젝트 규칙에 맞추면 나머지 컴포넌트는 동일한 응답 계약으로 동작한다.
